import { NotCopiableDirective } from './not-copiable.directive';

describe('NotCopiableDirective', () => {
  it('should create an instance', () => {
    const directive = new NotCopiableDirective();
    expect(directive).toBeTruthy();
  });
});
