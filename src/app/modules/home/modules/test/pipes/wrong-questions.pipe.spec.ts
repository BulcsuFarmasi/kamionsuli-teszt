import { WrongQuestionsPipe } from './wrong-questions.pipe';

describe('WrongQuestionsPipe', () => {
  it('create an instance', () => {
    const pipe = new WrongQuestionsPipe();
    expect(pipe).toBeTruthy();
  });
});
